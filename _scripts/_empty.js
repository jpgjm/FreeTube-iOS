// -----------------------------------------------------------------------------
// webpack `resolve.fallback` 用の空モジュール。
// `fs/promises` のような Node 専用 API が uReferenced になっても
// バンドルが失敗しないよう、空 exports にフォールバックさせる。
//
// (android 側の webpack.android.config.js も同じパスを参照しているが、
//  参照される code path が現状無いため上流リポジトリにこのファイルは
//  存在しない。iOS では明示的に用意してビルド堅牢性を上げる)
// -----------------------------------------------------------------------------

module.exports = {}
